{
  "name": "simple-web-app",
  "version": "1.0.0",
  "description": "Simple web application with GitHub Actions demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "echo 'Building application...' && mkdir -p dist && cp -r public dist/ && cp server.js dist/",
    "lint": "eslint ."
  },
  "keywords": ["nodejs", "express", "github-actions", "ci-cd"],
  "author": "Developer",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "eslint": "^8.50.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}